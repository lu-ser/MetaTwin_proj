{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Digital Twin Platform",
            "type": "python",
            "request": "launch",
            "module": "app.main",
            "cwd": "${workspaceFolder}"
        }
    ]
}